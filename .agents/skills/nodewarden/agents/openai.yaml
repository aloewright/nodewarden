interface:
  display_name: "Nodewarden"
  short_description: "Repo-specific patterns and workflows for nodewarden"
  default_prompt: "Use the nodewarden repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true